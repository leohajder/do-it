AppBundle\Entity\User:
    type:  entity
    table: fos_user
    id:
        id:
            type: integer
            generator:
                strategy: AUTO
    oneToMany:
        lists:
            targetEntity: TaskList
            mappedBy: user
            orphanRemoval: true
